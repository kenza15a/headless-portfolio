import React from "react";
import HeroSlider from "./HeroSlider";

const Hero = () => {
  return (
    <section className="h-max w-full px-0   " id="hero">
      <HeroSlider />
    </section>
  );
};

export default Hero;
